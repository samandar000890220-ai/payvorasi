YES — CONNECT TO MY PRIVATE GITHUB REPOSITORY AND HANDLE THE ENTIRE F5-TTS COMPATIBILITY INTEGRATION.

Repository: **F5-TTS-Voice-Studio**

IMPORTANT: DO NOT GUESS THE ADAPTER IMPLEMENTATION AND DO NOT CREATE A SECOND F5-TTS IMPLEMENTATION.

The F5-TTS adapter already exists and is running successfully on my Windows PC at:

http://127.0.0.1:7861

I have verified that the existing server exposes these routes:

/pronunciation
/pronunciation/api/list
/pronunciation/api/set
/pronunciation/api/delete
/pronunciation/api/reload
/health
/tts_to_audio/
/tts_to_audio
/create_and_store_latents/
/create_and_store_latents

I have also verified that `/openapi.json` returns HTTP 200.

YOUR TASK:

1. Connect to my private GitHub repository and inspect the ACTUAL `f5_adapter_server.py` source and all related F5-TTS adapter files.

2. Understand the existing implementation completely before making changes:

   * F5-TTS model loading
   * persistent inference
   * voice/reference handling
   * `/tts_to_audio`
   * `/create_and_store_latents`
   * pronunciation system
   * existing health endpoint
   * authentication/token handling
   * output/audio handling
   * CUDA/device configuration
   * existing command-line arguments

3. DO NOT replace the existing adapter.

4. DO NOT remove or rename any existing routes.

5. DO NOT break the existing SkyrimNet/F5-TTS functionality.

6. Add a COMPATIBILITY API layer for Payvora that exposes:

   GET /v1/health

   POST /v1/speech

   POST /v1/vocal-event

   POST /v1/transcribe

7. The new `/v1/*` endpoints must translate Payvora's existing request contract into the ACTUAL request format supported by the existing adapter.

8. `/v1/speech` must support:

   * text
   * reference audio
   * reference text
   * model
   * device
   * compute_type
   * controls JSON
   * speed
   * pitch
   * energy
   * emotion
   * response_format

9. DO NOT pretend that F5-TTS natively supports parameters that it does not support.

If pitch, energy, or other controls are not native to the existing F5-TTS pipeline, determine whether they can be implemented as genuine post-processing without damaging voice identity. If they cannot be implemented reliably, expose their actual capability status instead of silently pretending they work.

10. For emotion tags and vocal events such as `[laugh]` and `[sigh]`, inspect the actual implementation first.

Do NOT invent behavior.

If unsupported, return an explicit capability/error response that Payvora can handle gracefully.

11. `/v1/health` must return a useful health response showing:

* adapter reachable
* F5-TTS model loaded/not loaded
* CUDA availability
* current device
* supported capabilities
* adapter version

12. `/v1/speech` must return actual audio bytes generated by the existing F5-TTS pipeline.

NO MOCK AUDIO.
NO FAKE SUCCESS.
NO SAMPLE AUDIO.
NO STOCK VOICE FALLBACK.

13. `/v1/transcribe` must use the existing transcription capability if one exists. If transcription is not available, report that honestly rather than creating a fake implementation.

14. Preserve all existing routes and behavior.

15. Add automated tests for every new `/v1/*` endpoint.

16. Add request validation and clear HTTP errors for:

* missing text
* invalid reference audio
* missing/invalid voice
* unsupported controls
* model not loaded
* inference failure
* authentication failure

17. Authentication:

* Use `F5_TTS_ADAPTER_TOKEN` if the existing adapter already supports it.
* Do not hard-code secrets.
* Payvora will send `Authorization: Bearer <token>`.
* Reject unauthorized `/v1/*` requests with HTTP 401.
* Keep any existing authentication behavior intact.

18. Make the compatibility layer configurable through environment variables instead of hard-coding URLs, ports, tokens, model paths, or credentials.

19. IMPORTANT NETWORK ARCHITECTURE:

Payvora/Replit cannot directly access:

127.0.0.1:7861

The Windows F5-TTS machine therefore needs a secure tunnel.

The final architecture should be:

Payvora Voice Studio
→ Payvora API server
→ F5_TTS_SERVICE_URL
→ secure tunnel
→ Windows F5-TTS compatibility API
→ existing F5-TTS adapter
→ CUDA/F5-TTS model
→ generated audio

20. Do NOT ask me to manually rewrite the adapter if the repository contains the actual source.

Inspect the repository and implement the compatibility layer yourself.

21. Do NOT redesign Voice Studio.

Do NOT create a new Text-to-Speech page.

Do NOT modify the existing Voice Studio visual design.

This task is BACKEND/API COMPATIBILITY ONLY.

22. After implementation, run:

* type checks
* lint
* unit tests
* API contract tests
* build

23. Show me the exact files changed and explain exactly how the new `/v1/*` endpoints map to the existing adapter.

24. Most importantly:

DO NOT CLAIM THE INTEGRATION WORKS UNTIL YOU HAVE TESTED THE ACTUAL API CONTRACT AGAINST THE REAL ADAPTER IMPLEMENTATION.

If the private GitHub connection is required to inspect the real adapter source, use that connection now.

Do not create a speculative proxy based only on `/openapi.json`.

FIRST inspect the actual repository → THEN implement → THEN test → THEN report.
